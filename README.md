# CLI-Animator
# WORK IN PROGRESS - currently WINDOWS-only
# has a lot of BLOAT
# not optimized
# needs FFmpeg to run

CLI-Animator is a program which let's you run Videos in Terminal. Currently WORK IN PROGRESS & Windows-only.
